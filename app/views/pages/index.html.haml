%h2 Hello world

= image_tag @user.image_url if @user.image.present?

//= link_to "Delogare", destroy_user_session_path, method: :delete, class: "btn btn-default"

= link_to "Edit", edit_user_registration_path, class: "btn btn-default"
