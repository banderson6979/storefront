-content_for :head do
  = t ".head"

-content_for :mail do
  = @resource.email

-content_for :content_head do
  = t ".content_head"

-content_for :content do
  %p= t ".details"
  %p= link_to (t ".confirm_account"), confirmation_url(@resource, confirmation_token: @token)
