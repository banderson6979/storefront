- content_for :head do
  = t ".head"

- content_for :mail do
  = @resource.email

- content_for :content_head do
  = t ".content_head"

- content_for :content do
  %p= t ".details"
  %p= link_to (t ".change_password"), edit_password_url(@resource, reset_password_token: @token), style: "color: red"
  %p= t ".details_to_ignore"
  %p= t ".details_to_change"
