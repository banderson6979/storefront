%p= t ".head"
%p= @resource.email
%p= t ".details"
%p= link_to (t ".change_password"), edit_password_url(@resource, reset_password_token: @token)
%p= t ".details_to_ignore"
%p= t ".details_to_change"
